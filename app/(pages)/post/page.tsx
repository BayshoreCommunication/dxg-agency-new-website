"use client";
import React from "react";

const page = () => {
  // const blogsData = await GetAllBlogPost().then((res) => res);
  // console.log(blogsData);
  return <div>page</div>;
};

export default page;
